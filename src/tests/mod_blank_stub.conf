# Lighttpd config that attempts to load only mod_blank.  (("mod_indexfile", "mod_dirlisting", "mod_staticfile", "mod_chunked") are always loaded.)
############ Options you really have to take care of ####################

server.modules = ( )
server.port = 8080
server.document-root = "./"
